apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: "$INFLUXDB_DB"
    user: "$INFLUXDB_USER"
    password: "$INFLUXDB_PASSWORD"
    url: "$INFLUXDB_URL"
    isDefault: true
    editable: true
